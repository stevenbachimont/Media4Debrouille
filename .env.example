# Database (SQLite dev / PostgreSQL prod)
DATABASE_URL="file:./dev.db"

# Player JWT (HMAC-SHA256)
PLAYER_JWT_SECRET="change-me-in-production-min-32-chars"

# S3 / Object Storage (optionnel en dev)
S3_BUCKET=""
S3_REGION=""
S3_ACCESS_KEY=""
S3_SECRET_KEY=""
CDN_BASE_URL=""

# Lucia Auth (sessions)
# Générer avec: openssl rand -base64 32
LUCIA_SECRET=""

# Version player attendue (badge mise à jour)
CURRENT_PLAYER_VERSION="1.0.0"
